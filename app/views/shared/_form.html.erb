<% div_for resource do %>
	<%= f.error_messages %>
	
	<% f.inputs do %>
		<%= f.input :name %>
	<% end %>
	
	<% f.buttons do %>
		<%= f.commit_button "Save #{resource_class.to_s.downcase}" %> or <%= link_to 'Cancel', root_path, :class => :cancel %>
	<% end %>
<% end %>