<h1>Editing <%= @district.name %></h1>

<% simple_form_for @district do |f| %>
	<%= render 'shared/form', :f => f %>
	
	<div class="actions">
    <%= f.button :submit, "Save changes", class: "btn primary" %>
    <%= link_to "Cancel", districts_path, class: "btn" %>
  </div>
<% end %>