<h1>Editing <%= "#{@user.full_name}'s Profile" %></h1>

<%= simple_form_for @user do |f| %>
  <%= render 'form', f: f %>
    
  <div class="actions">
    <%= f.button :submit, "Save changes", class: "btn primary" %>
    <%= link_to "Cancel", users_path, class: "btn" %>
  </div>
<% end %>