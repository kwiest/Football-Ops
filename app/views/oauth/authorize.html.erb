<h1>Authorize <%= @app.name %></h1>

<p><strong>
  <%= current_user.name %>, <%= link_to @app.name, @app.website %> wants to access your 
  FootballOps.org data.
</strong></p>

<p>
  This app will have full read/write access to your information, so only click 'Allow' if you
  recognize and trust this application. You can also revoke access to this or any other
  application at any time in your <%= link_to 'account settings' %>.
</p>

<p>Don't recognize this app? Simply click 'Deny' below.</p>

<%= form_tag '/oauth/authorize', style: 'display: inline-block' do %>
  <%= hidden_field_tag :api_key, @app.api_key %>
  <%= hidden_field_tag :authorize, false %>
  <%= hidden_field_tag :redirect_uri, @redirect_uri %>
  <%= hidden_field_tag :state, @state %>
  <%= submit_tag 'Deny', class: 'btn' %>
<% end %>

<%= form_tag '/oauth/authorize', style: 'display: inline-block;' do %>
  <%= hidden_field_tag :api_key, @app.api_key %>
  <%= hidden_field_tag :authorize, true %>
  <%= hidden_field_tag :redirect_uri, @redirect_uri %>
  <%= hidden_field_tag :state, @state %>
  <%= submit_tag 'Allow', class: 'btn btn-primary' %>
<% end %>
